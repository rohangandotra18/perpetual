# Copy to .env and paste the Cluster0 URI from Atlas → Connect → Drivers.
# Do not commit .env — this GitHub repo is public.
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.<cluster-id>.mongodb.net/?appName=Cluster0
MONGODB_DB=hackathon
