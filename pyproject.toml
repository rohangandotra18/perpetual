[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "perpetual"
version = "0.1.0"
description = "An agent whose tool list is a MongoDB $vectorSearch result"
requires-python = ">=3.11"
readme = "README.md"

[tool.setuptools.packages.find]
where = ["src"]
